package main

import "fmt"

func main() {
	s := []int{10, 20, 30}

	s[1] = 25
	fmt.Println(s)
}

// In this code, we have declared an array arr with 5 string elements. We then use a for loop to iterate over the array and print each element. The output will show each string in the array on a new line.
