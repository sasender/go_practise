### What is interfaces?
*** Interfaces give polymorphism and decoupling, which helps scale and maintain code — not direct code reuse. ***

*** we are not calling the interfaces, we called the functions which are accepets the interfaces ***

*** Interfaces give polymorphism and decoupling, which helps scale and maintain code — not direct code reuse. ***

*** Function parameters are the inputs a function receives — like values passed into it when you call it. ***

*** An interface in Go defines a set of methods. Any type that has those methods can be used where the interface is expected. ***

*** Interfaces only declare method signatures ***

*** In Go, we don’t call interfaces — we pass concrete values to functions that expect interfaces. ***

